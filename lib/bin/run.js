import fs from"node:fs/promises";import path from"node:path";import YAML from"yaml";import*as Project from"#project/project";const dir=process.argv[process.argv[2]===`run`?3:2];dir&&process.chdir(dir);const type=path.basename(path.dirname(process.cwd())).toLowerCase(),config=YAML.parse(await fs.readFile(`config.yml`,`utf8`)),project=new(await(Project[type][config.name]())).Project(config);project.manager.start();