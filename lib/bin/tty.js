import fs from"node:fs/promises";import Path from"node:path";import YAML from"yaml";import*as Philia from"#project/project/philia.js";import Tui from"#protocol/tty";const path=Path.join(`Project`,`TTY`);await fs.mkdir(path,{recursive:!0}),process.chdir(path);const config=YAML.parse(await fs.readFile(`config.yml`,`utf8`).catch(()=>``))||await Philia.Project.createConfig(`App`);new Tui(config).main();